# Highlights

# Logs
```dataviewjs 
// Get the current file name and extract the first 7 characters 
let currentPrefix = dv.current().file.name.substring(0, 7);
let pages = dv.pages('"Daily"').sort(p => p.file.name, "asc");
// Loop through pages 
for (let p of pages) {
	let fileName = p.file.name;
	  // Check if the file name starts with "2024-01"
	  if (fileName.startsWith(currentPrefix) ) {
		let noteText = await dv.io.load(p.file.path); 
		let regexPattern = /#{1,6}\s(TOP-OF-MIND)\n(.+?)(?:\n#{1,6}|$)/sgi; 
		let matches = regexPattern.exec(noteText); 
		if (matches != null) {
			let sectionName = matches[1]; 
			let sectionText = matches[2]; 
			// Insert into document however you like 
			dv.paragraph(dv.fileLink(fileName));
			dv.el("article", sectionText);
		}
	}
}
```